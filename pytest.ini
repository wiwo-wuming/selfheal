[pytest]
testpaths = tests
addopts = --cov=src/selfheal --cov-report=term-missing --cov-report=xml --cov-branch
pythonpath = src
    tests
markers =
    vcr: tests using vcrpy to record/replay HTTP interactions
